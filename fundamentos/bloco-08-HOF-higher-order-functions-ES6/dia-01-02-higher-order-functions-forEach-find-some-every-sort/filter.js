//ARRAY.FILTER - 

/* const mensagem = (usuario) => {
    const varOne = `${usuario.email} foi aprovado com ${usuario.nota}`;
    const varTwo = `${varOne}! Parabéns, ${usuario.nome}!`;
    console.log(varTwo);
}

const listaDePessoasAprovadas = [
    {nome: 'Alex', email: 'alex@gmail.com', nota: 100},
    {nome: 'Andre', email: 'andre@gmail.com', nota: 90},
    {nome: 'Arthur', email: 'arthur@gmail.com', nota: 110},
    {nome: 'Giovana', email: 'giovanna@gmail.com', nota: 80},
    {nome: 'Alessa', email: 'alessa@gmail.com', nota: 120},
]

const pessoasComNotaAlta = listaDePessoasAprovadas.filter( (usuario) => usuario.nota >= 100);

pessoasComNotaAlta.forEach( (usuario) => mensagem(usuario)); */


/* const numbers = [1, 3, 60, 32, 42, 79, 53]; */

/* const verificacaoPar = (numero) => numero % 2 === 0;

const verifica = numbers.filter(verificacaoPar);

console.log(verifica); */

/*const isPar = numbers.filter( (number) => number % 2 === 0);

console.log(isPar); */


/* const objPeople = [
    { name: 'José', age: 21 },
    { name: 'Lucas', age: 19 },
    { name: 'Maria', age: 16 },
    { name: 'Gilberto', age: 18 },
    { name: 'Vitor', age: 15 },
  ];

  const idadeMinima = (minimo) => {
    return minimo.filter( (usuario) => usuario.age < 18);
  }

  
  console.log(idadeMinima(objPeople)); */


/*   const arrayMyStudents = ['Maria', 'Manuela', 'Jorge', 'Ricardo', 'Wilson'];

  const semRicardo = (name, lista) => {
      return lista.filter( (element) => element !== name);
  }

  console.log(semRicardo('Ricardo', arrayMyStudents)); */


